public class Main {
    public static void main(String[] args) {
        // HashMap<Integer, Character>
    }
    // HashMap
    // put() == insert, update (left, right)
    // get(key) == value(right side)
    // containsKey(key) ==  boolean
    // keySet() == set 
    // remove(key) == deletes the key
}
